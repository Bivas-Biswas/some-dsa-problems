// https://codeforces.com/problemset/problem/367/B

#include<bits/stdc++.h>
using namespace std;

void solve(){
    int n, m, p;
    cin >> n >> m >> p;
    int a[n], b[m];
    for(int i=0; i<n; i++){
        cin >> a[i];
    }

    for(int i = 0l; i<m; i++){
        cin >> b[i];
    }

    
}

signed main(){
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    int t=1;
    while(t--) solve();
}
